# 💬 موظف الاستقبال الذكي — الدليل

البزنس: بتبيع لمحلات بعمّان **موظف استقبال بيرد على زباينهم بثواني، ٢٤ ساعة**، باشتراك شهري.

---

## شغّله هلأ وجرّبه

```bash
npm install
cp .env.example .env.local     # حط ANTHROPIC_API_KEY
npm run dev
```

افتح **http://localhost:3000/desk** واحكي معه كأنك زبون:

- `قديش سعر تنظيف الأسنان؟` → بيرد بالسعر الحقيقي من إعدادات المحل
- `بدي أحجز كشفية بكرا` → بيشوف الأوقات الفاضية فعلياً وبيحجز
- `بتفتحوا الجمعة؟` → بيرد حسب الدوام المسجّل
- `عندي وجع شديد، شو الدوا؟` → **ما بيرد** — بيحوّلها إلك

آخر وحدة هي الأهم. جرّبها.

---

## ✅ شو مبني فعلاً

| الملف | شو بيعمل |
|---|---|
| `lib/desk/db/` | **طبقة التخزين** — واجهة وحدة، محوّل Supabase ومحوّل ذاكرة للتطوير |
| `supabase/migrations/` | الـ schema كامل: ١٦ جدول + RLS على كل واحد |
| `lib/desk/escalation.ts` | **بوابة السياسات** — بتفحص كل رسالة *قبل* ما يشوفها الموديل |
| `lib/desk/bookings.ts` | التوفر والحجز: بيرفض الماضي والمسكّر والمحجوز والعطل |
| `lib/desk/receptionist.ts` | الوكيل اللي بيحكي مع الزبون |
| `lib/desk/pipeline.ts` | خط المعالجة الموحّد لكل القنوات + تسجيل المهام والأحداث |
| `lib/desk/agents.ts` | سجل الوكلاء — كل واحد معلّم `live` أو `planned` |
| `lib/desk/whatsapp.ts` | قناة واتساب — كاملة، بتشتغل أول ما يوجد الحساب |
| `app/desk` | لوحة التجربة |
| `app/api/desk/whatsapp` | مستقبِل رسائل واتساب مع تحقق من توقيع ميتا |

**٤٤ فحص آلي** بتغطي كل قاعدة صارمة: `npm run test:desk`

### القاعدتين اللي بتحميك

**١. المشرف بيشتغل قبل الموديل.** رسالة فيها سؤال طبي أو شكوى أو طلب استرجاع أو خصم **ما بتوصل للموديل أصلاً** — بتروح لبني آدم. ما في صيغة كلام بتتجاوزها، لأنها قواعد ثابتة مش حكم الموديل.

**٢. الموديل ما بيقرر إذا الوقت فاضي.** بيسأل `bookings.ts`، وهي بترفض أي وقت مسكّر أو محجوز أو راح. **موعد ما بتقدر تحترمه أسوأ من لا موعد.**

---

## اربط قاعدة البيانات ⏱️ ١٠ دقايق

بدون هالخطوة كل حجز بينمسح مع أول نشر جديد، واللوحة بتكتبلك هاد بشريط أحمر.

1. أنشئ مشروع جديد على **supabase.com**
2. افتح **SQL Editor** → `New query`
3. انسخ محتوى `supabase/migrations/0001_receptionist_core.sql` **كله** والصقه → **Run**
4. **مهم:** شغّل `supabase/seed_demo.sql` كمان — بدونه قاعدة البيانات بتكون فاضية واللوحة بتطلع بلا أنشطة
5. من **Settings → API** خُد القيمتين وحطهم بمتغيرات البيئة:

```bash
SUPABASE_URL=https://<ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service_role — للسيرفر فقط>
```

> 🔐 مفتاح `service_role` بيتجاوز قيود RLS بالتصميم، لأنه السيرفر بيشتغل نيابة عن المحل اللي وجّهته القناة إله. **ولا مرة تحطه بكود يشوفه المتصفح.**

بتعرف إنها زبطت لما الشريط الأحمر يختفي من `/desk`.

**ليش قيد بقاعدة البيانات مش منطق بالكود؟** منع الحجز المزدوج معمول كـ `EXCLUSION CONSTRAINT` على نطاق الوقت. يعني لو وصلوا طلبين بنفس اللحظة، مستحيل ينجحوا الاثنين — مهما كبّرت النظام أو شغّلت أكتر من نسخة.

---

## أضف زبونك الأول

حط بـ `.env.local`:

```bash
DESK_TENANTS='[{
  "id": "clinic-samer",
  "name": "عيادة د. سامر",
  "kind": "clinic",
  "timezone": "Asia/Amman",
  "city": "عمّان",
  "address": "شارع مكة، عمّان",
  "phone": "+962 7 9000 0000",
  "hours": {
    "sun": {"open":"09:00","close":"17:00"},
    "mon": {"open":"09:00","close":"17:00"},
    "tue": {"open":"09:00","close":"17:00"},
    "wed": {"open":"09:00","close":"17:00"},
    "thu": {"open":"09:00","close":"15:00"},
    "fri": null,
    "sat": {"open":"10:00","close":"14:00"}
  },
  "services": [
    {"id":"check","name":"كشفية","durationMin":30,"price":20,"currency":"JOD"},
    {"id":"clean","name":"تنظيف أسنان","durationMin":45,"price":35,"currency":"JOD"}
  ],
  "facts": [{"q":"التأمين","a":"بنقبل تأمين طبي، احكي مع الاستقبال قبل الموعد بيوم."}],
  "policies": {"cancellationHours":4,"walkIns":false},
  "escalation": {"topics":["تأمين خاص"],"ownerContact":"د. سامر"},
  "leadTimeMin": 60, "slotStepMin": 15, "horizonDays": 30
}]'
```

> ⚠️ **خُد كل معلومة من صاحب المحل نفسه ومكتوبة.** سعر غلط هون = الوكيل بيقوله للزبون بثقة.

اللوحة بتفحص الإعدادات وبتحكيلك إذا في غلط (دوام مقلوب، منطقة زمنية خطأ، خدمة مكررة).

---

## 🔊 خلّيه يحكي بصوت بشري ⏱️ ٥ دقايق

بدون إعداد، الصوت اللي بتسمعه هو صوت المتصفح — على الماك هاد الصوت الآلي
المعروف، وأي زبون بيعرفه من أول كلمة. مش مناسب لعمل بتدفع عشان يبان محترف.

أي **واحد** من التلاتة بيكفي:

### Azure — الخيار الموصى فيه للأردن والخليج

هو الوحيد اللي عنده صوت **أردني حقيقي**، ومنحته المجانية الأكبر
(٥٠٠ ألف حرف بالشهر).

1. افتح [portal.azure.com](https://portal.azure.com) → **Create a resource** →
   دوّر **Speech** → Create
2. اختار Region: `uaenorth` (الأقرب) أو `westeurope`، والخطة **F0 (مجانية)**
3. بعد ما يخلص: **Keys and Endpoint** → انسخ `KEY 1` و`Location/Region`
4. بـVercel → Settings → Environment Variables، ضيف:

| المتغيّر | القيمة |
| --- | --- |
| `AZURE_SPEECH_KEY` | المفتاح اللي نسخته |
| `AZURE_SPEECH_REGION` | `uaenorth` |
| `AZURE_SPEECH_VOICE` | `ar-JO-TaimNeural` (شب) أو `ar-JO-SanaNeural` (بنت) |

أصوات تانية: `ar-SA-HamedNeural` و`ar-SA-ZariyahNeural` (سعودي)،
`ar-AE-FatimaNeural` (إماراتي)، `ar-EG-ShakirNeural` (مصري).
اسمعهم كلهم قبل ما تختار على
[speech.microsoft.com/portal/voicegallery](https://speech.microsoft.com/portal/voicegallery).

### ElevenLabs — الأطبع بالصوت، بس فصحى

أحلى صوت من ناحية الطبيعية، بس بيحكي فصحى مش لهجة. مجاناً ١٠ آلاف حرف بالشهر.

1. [elevenlabs.io](https://elevenlabs.io) → Profile → **API Key**
2. ضيف `ELEVENLABS_API_KEY`، وإذا بدك صوت معيّن `ELEVENLABS_VOICE_ID`

### OpenAI — الأسهل إذا عندك مفتاح

بيشتغل، بس لكنته أجنبية واضحة بالعربي. ضيف `OPENAI_API_KEY`.

---

**بعد ما تضيف أي واحد منهم:** Redeploy، وافتح `/desk` واضغط 🔈.
الشريط تحت بيقلك بالضبط أي صوت عم تسمع — مثلاً
`🔊 Azure — صوت أردني عصبي (ar-JO-TaimNeural)`. إذا لسا مكتوب
`🤖 صوت المتصفح الآلي`، يعني المفتاح ما وصل.

إذا حطّيت أكتر من مفتاح، الترتيب: Azure ← ElevenLabs ← OpenAI.
تقدر تفرض واحد بـ`TTS_PROVIDER=elevenlabs`.

**الكلفة:** رد الاستقبال بحدود ١٥٠ حرف. يعني منحة Azure المجانية بتكفي
~٣٣٠٠ رد بالشهر. والردود المكرّرة («أهلاً وسهلاً») بتتخزّن بالذاكرة وما
بتنحسب مرتين.

**مهم:** هاد صوت بالمتصفح، مش خط تلفون. الرد على مكالمة حقيقية بدّو مزوّد
اتصالات (Twilio أو مثله) وهاد لسا ما انبنى — بس نفس الأصوات بتشتغل عليه
لما نوصله.

## اربطه بواتساب

هاي الخطوة الوحيدة اللي **بدها منك** — الحساب لازم يكون باسمك، ميتا بتطلب هوية وبيانات عمل.

1. `business.facebook.com` → أنشئ Business Account
2. `developers.facebook.com` → New App → **Business** → ضيف **WhatsApp**
3. خُد: `WHATSAPP_TOKEN` · `WHATSAPP_PHONE_NUMBER_ID` · `WHATSAPP_APP_SECRET`
4. اختر أي نص لـ `WHATSAPP_VERIFY_TOKEN` وحطه بالمكانين
5. **Webhook URL:** `https://<موقعك>/api/desk/whatsapp` → اشترك بحقل `messages`
6. اربط الرقم بالمحل:
   ```bash
   DESK_WHATSAPP_ROUTES='{"1234567890":"clinic-samer"}'
   DESK_OWNER_WHATSAPP=9627xxxxxxxx
   ```

من هون وجاي، أي رسالة بتوصل رقم المحل بيرد عليها الوكيل — والتحويلات بتوصلك على واتساب.

**الأمان:** كل رسالة من ميتا بينتحقق من توقيعها قبل ما ينعمل فيها إشي. بدون `WHATSAPP_APP_SECRET` بتشتغل بس بتطبع تحذير — **حطه**.

---

## ❌ شو لسا ما انبنى

بصراحة، عشان تعرف وين إنت:

| | الحالة |
|---|---|
| **الوكيل الصوتي** (يرد على التلفون) | **ما انبنى.** بدو مزوّد اتصالات + صوت عربي. أكبر فجوة وأعلى تسعيرة — هاي الخطوة الجاي |
| **ربط التقويم** (جوجل كالندر) | ما انبنى |
| **إنستغرام** | ما انبنى — بس خط المعالجة جاهز يستقبله بنفس النواة |
| **الكشّاف والمُراسِل والمحلل** | ما انبنوا |
| **لوحة للعميل** | ما انبنت |
| **عالم الوكلاء ثلاثي الأبعاد** | نسخة أولى موجودة، بس لسا مش موصولة بالأحداث الحقيقية |

---

## 💰 التسعير

| | سعر شهري معقول |
|---|---|
| واتساب بس | **٣٠–٨٠ دينار** |
| واتساب + تلفون | **١٥٠–٤٠٠ دينار** |

الفرق كبير لأنه الرد على التلفون بيوفّر **راتب موظف كامل**.

**٢٠ زبون على واتساب = ٦٠٠–١٦٠٠ دينار شهرياً متكرر**، وتكلفة الزبون الإضافي شبه صفر.

### كيف تسكّر أول زبون
ركّبه لواحد **ببلاش أسبوعين**. بعدها فرجيه سجل الرسائل اللي انردّت وهو نايم. النتيجة بتبيع حالها أحسن من أي كلام.

---

## الأوامر

```bash
npm run dev          # شغّل، بعدين /desk
npm run test:desk    # ٤٤ فحص للقواعد الصارمة
npm run typecheck
npm run build
```
